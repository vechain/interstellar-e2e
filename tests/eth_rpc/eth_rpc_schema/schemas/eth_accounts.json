{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "eth_accounts.json",
  "title": "eth_accounts result",
  "description": "Addresses owned by the client. Thor holds no node-side keystore, so this is always an empty array.",
  "type": "array",
  "items": { "$ref": "_defs.json#/$defs/address" }
}
